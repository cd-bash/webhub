import { homeMeta } from '../home/meta';
import { aboutMeta } from '../about/meta';
import { contactMeta } from '../contact/meta';
import { logsMeta } from '../logs/meta';

export const pageMeta = {
  home: homeMeta,
  about: aboutMeta,
  contact: contactMeta,
  logs: logsMeta
};